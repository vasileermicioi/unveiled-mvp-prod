# pagination-seed

Extend the operations smoke seed script with a deterministic pagination dataset (--profile pagination, --reset) so admin and discovery pagination controls can be exercised locally and in CI.
