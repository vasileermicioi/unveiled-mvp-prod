# retire-heroui-replica

Delete the Ladle-only HeroUI replica folder, its gate script, its unit test, and the heroui-replica package export; production atoms/molecules are now the visual source of truth.
