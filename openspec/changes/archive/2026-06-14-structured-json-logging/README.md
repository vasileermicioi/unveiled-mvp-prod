# structured-json-logging

Production observability: replace the unstructured logger with a structured JSON logger so that every log line emitted by the Astro SSR worker, Astro Actions, and HTTP endpoints can be aggregated, queried, and alerted on by a log pipeline (Datadog/Loki/Honeycomb/etc.).
