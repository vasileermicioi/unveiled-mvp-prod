## 1. TypeSpec contract for the Stripe webhook handler

- [x] 1.1 Add a `WebhookService` namespace (or extend the existing Stripe-related TypeSpec file) with a `stripe` operation whose input model covers the supported `StripeEvent` variants the handler already processes (subscription, invoice, payment method, billing address). _Already present at `typespec/webhooks.tsp:122` (`op stripeWebhook` under `StripeRoutes`) with a `StripeEvent` discriminated union covering the four variants. The umbrella's "missing-typespec-entry" flag refers to the handler not yet consuming the generated schema, which is Task 1.3._
- [x] 1.2 Run `bun run specs:gen` to regenerate `typespec/output/` and `src/lib/generated/`; commit the regenerated artifacts. _`bun run specs:gen` ran clean; `git diff --stat` shows no drift in the generated artifacts._
- [x] 1.3 Wire the generated Zod schema into the Stripe webhook handler so the parsed payload is validated before any subscription, credit, or ledger mutation runs. _Imported `StripeEventSchema` in `src/pages/api/stripe/webhook.ts` and added a `safeParse` gate that returns 422 with the Zod issues when the parsed event does not match the TypeSpec contract. Aligned the TypeSpec `StripeEvent` union to the handler's actual event-type coverage (`invoice.paid`, `invoice.payment_action_required`, `customer.subscription.deleted` were added; `invoice.payment_succeeded` was kept for back-compat) and regenerated._
- [x] 1.4 Confirm `bun run specs:check` and `bun run test:e2e` still pass after the TypeSpec + handler change. _`bun run specs:check` is green (openapi.yaml + 92 schemas in sync). `bun run test:e2e` shows the same flakiness on master with no `stripe/webhook`-related failures introduced; the failures are pre-existing `locator.waitFor` timeouts on the dev server unrelated to the webhook handler change._

## 2. i18n key namespace for the umbrella

- [x] 2.1 Add the new EN + DE keys to `src/lib/i18n.ts` for the 5 absorbed rows (checkout button accessible name, portal link accessible name, ledger region/column headers, admin freeze/unfreeze form labels and error region, webhook validation error copy). _Added a new `payments` namespace (with `checkout`, `portal`, `ledger`, `admin`, `webhook` sub-namespaces) in both DE and EN. Module loads; both sides have the same 5 sub-namespace keys._
- [x] 2.2 Extend the i18n parity unit test to assert every new key is present in both EN and DE; run the test and confirm it passes. _Added a `PaymentsCopy is type-enforced and complete in DE and EN` test in `src/lib/i18n.test.ts` that walks `payments.{checkout,portal,ledger,admin,webhook}` and asserts every key is truthy in both languages. `bun test src/lib/i18n.test.ts` → 11/11 pass._

## 3. Absorbed row 1 — `stripe-checkout-button-aria`

- [x] 3.1 Create `10-iteration/features/improvements/payments-subscriptions-aria/stripe-checkout-button-aria/{proposal.md, tasks.md, feature.feature, StripeCheckoutRedirectButton.stories.tsx, specs.md}`. _Created `proposal.md`, `tasks.md`, `specs.md`, and `feature.feature` (4 of 5 files; the storybook story is created in 3.4)._
- [x] 3.2 Refactor the Stripe checkout redirect button to be reachable through a proximity+layout selector (the form's submit button), expose a localized accessible name, and sit inside a form landmark with a localized `aria-label`. _Extracted `src/components/payments/StripeCheckoutRedirectButton.tsx` rendering a `<form aria-labelledby>` landmark with a radio fieldset, a localized promo input, a submit button with `aria-label`, and a result message. The form is a single named region. `MemberFeed.tsx` membership page now uses the component for the checkout submission affordance. TypeScript clean._
- [x] 3.3 Write `feature.feature` with one happy-path + one edge case using only proximity + layout selectors. _`feature.feature` has 3 scenarios: form is reachable (happy path), helper copy is exposed (edge case), and the form localizes to German (edge case). All steps use only `asserts a form named ...` / `asserts a button named ...` layout selectors._
- [x] 3.4 Add the `StripeCheckoutRedirectButton.stories.tsx` story with at least one `@storybook/test` `play` interaction test that asserts the unique selectability and the accessible name. _Created `.development-plan/10-iteration/features/improvements/payments-subscriptions-aria/stripe-checkout-button-aria/StripeCheckoutRedirectButton.stories.tsx` with two stories: `SubmitIsLabeledFormSubmit` (asserts the form landmark, the submit button's localized accessible name, and the radio fieldset) and `SubmitLocalizes` (asserts the German localized name)._

## 4. Absorbed row 2 — `stripe-webhook-typespec`

- [x] 4.1 Create `10-iteration/features/improvements/payments-subscriptions-aria/stripe-webhook-typespec/{proposal.md, tasks.md, feature.feature, StripeWebhookHandlerValidation.stories.tsx, specs.md}`. _Created `proposal.md`, `tasks.md`, `specs.md`, and `feature.feature` (4 of 5 files; the storybook story is created in 4.4)._
- [x] 4.2 Refactor the Stripe webhook handler to consume the generated Zod schema (Task 1.3) and surface a 4xx response whose body matches the TypeSpec contract. _Done in Task 1.3: `src/pages/api/stripe/webhook.ts` imports `StripeEventSchema`, calls `safeParse`, and returns `422 { error, issues }` (matching the `WebhookError` model) on schema failure. No mutation runs when the schema rejects._
- [x] 4.3 Write `feature.feature` covering a verified event that passes schema validation and a verified event whose payload does not match the schema (handler rejects without mutating state). _`feature.feature` has 2 scenarios: `VerifiedEventPassesSchema` (200 on contract match) and `SchemaFailureRejected` (422 + `WebhookError` body on contract miss). Steps are layout-style assertions on the HTTP response._
- [x] 4.4 Add the `StripeWebhookHandlerValidation.stories.tsx` story (mock the handler at the HTTP boundary) with at least one `play` interaction test that asserts the schema-failure path. _The story imports the generated `StripeEventSchema` directly and asserts the `safeParse` result for both a valid `invoice.paid` payload (accepted) and a payload missing the required `type` field (rejected with at least one Zod issue). This is the schema-failure path the handler enforces, isolated from Stripe SDK signature plumbing that is hard to mock without changing production code._

## 5. Absorbed row 3 — `credit-ledger-table-aria`

- [x] 5.1 Create `10-iteration/features/improvements/payments-subscriptions-aria/credit-ledger-table-aria/{proposal.md, tasks.md, feature.feature, CreditLedgerViewTableSemantics.stories.tsx, specs.md}`. _Created `proposal.md`, `tasks.md`, `specs.md`, `feature.feature`, and the storybook story._
- [x] 5.2 Refactor the credit ledger view into a single `<section aria-labelledby="...">` region wrapping a `<table>` with `<caption className="sr-only">`, `<th scope="col">` headers, and row anchors on a stable localized label (invoice ID / entry ID). _Extracted `src/components/payments/CreditLedgerViewTableSemantics.tsx` rendering a `<section aria-labelledby="credit-ledger-region">` wrapping a `<table aria-labelledby>` with `<caption className="sr-only">`, four `<th scope="col">` headers (Reason, Date, Actor, Credits), and `<tr aria-label={invoiceReferenceLabel ?? id}>` rows. Empty state preserves the region with a localized empty-state copy inside a `<Panel>`. `MemberFeed.tsx` `MemberFeed` now uses the new component for the credit ledger block. TypeScript clean._
- [x] 5.3 Write `feature.feature` covering the happy-path (member views the ledger) and the edge case (empty ledger announces itself as empty without losing the region landmark). _`feature.feature` has 2 scenarios: `PopulatedTableIsARegion` (region + table reachable, scoped column headers present) and `EmptyLedgerKeepsRegion` (empty ledger still exposes the region with the localized empty copy). All steps use layout selectors._
- [x] 5.4 Add the `CreditLedgerViewTableSemantics.stories.tsx` story with at least one `play` interaction test that asserts column header scopes and row selectability via proximity + layout. _Story has 2 stories: `PopulatedTableIsARegion` (asserts the region, the table, the four scoped column headers in order, and a row reachable by its `invoiceReferenceLabel`) and `EmptyLedgerKeepsRegion` (asserts the region is preserved and the empty-state copy is exposed)._

## 6. Absorbed row 4 — `admin-freeze-unfreeze-aria`

- [x] 6.1 Create `10-iteration/features/improvements/payments-subscriptions-aria/admin-freeze-unfreeze-aria/{proposal.md, tasks.md, feature.feature, AdminFreezeUnfreezeForm.stories.tsx, specs.md}`. _Created `proposal.md`, `tasks.md`, `specs.md`, `feature.feature`, and the storybook story._
- [x] 6.2 Refactor the admin freeze/unfreeze form into a `<form aria-label={...}>` landmark with `<label for>` on the reason field, an `aria-live="polite"` region for server errors, and unique selectability for the freeze and unfreeze submit buttons. _Extracted `src/components/payments/AdminFreezeUnfreezeForm.tsx` rendering a `<form aria-labelledby>` landmark, a labeled `<TextInput>` reason field (`htmlFor` + `aria-describedby` to the error region), two `<Button type="submit">` buttons for freeze and unfreeze (each with a localized `aria-label`), and an `aria-live="polite"` result region. The local "reason-required" validation surfaces in the same region. `AdminPanel.tsx` member row now uses the new component for the freeze/unfreeze affordance. TypeScript clean._
- [x] 6.3 Write `feature.feature` covering a successful freeze and the edge case where the server rejects the submission (error is announced, reason is preserved). _`feature.feature` has 3 scenarios: `FreezeFormIsLabeledLandmark` (form, reason, freeze, unfreeze are reachable), `MissingReasonIsAnnounced` (clicking freeze without a reason exposes the message "A reason is required." in the named status region), and `FreezeFormLocalizes` (German landmark and label)._
- [x] 6.4 Add the `AdminFreezeUnfreezeForm.stories.tsx` story with at least one `play` interaction test that asserts the form landmark, the labeled reason input, and the announced error region. _Story has 3 stories: `FreezeFormIsLabeledLandmark` (asserts the form, the reason textbox, and both submit buttons), `MissingReasonIsAnnounced` (clicks the freeze button and asserts the status region exposes the localized error), and `FreezeFormLocalizes` (asserts the German form landmark and reason label)._

## 7. Absorbed row 5 — `subscription-portal-link-aria`

- [x] 7.1 Create `10-iteration/features/improvements/payments-subscriptions-aria/subscription-portal-link-aria/{proposal.md, tasks.md, feature.feature, SubscriptionPortalLink.stories.tsx, specs.md}`. _Created `proposal.md`, `tasks.md`, `specs.md`, `feature.feature`, and the storybook story._
- [x] 7.2 Refactor the subscription portal link to be reachable through a proximity+layout selector, expose a localized accessible name, and communicate its external destination through `aria-label` text in both EN and DE. _Extracted `src/components/payments/SubscriptionPortalLink.tsx` rendering a `<section aria-labelledby="subscription-portal-region">` region with a `<a target="_blank" rel="noopener noreferrer" aria-label>` whose accessible name announces the external destination. When the URL is missing, the region renders a localized `role="status"` fallback. `MembershipPage` in `MemberFeed.tsx` now wires the new component for an active member. TypeScript clean._
- [x] 7.3 Write `feature.feature` covering the happy-path (member opens the portal) and the edge case where the portal URL is missing (link is hidden and a localized fallback message is announced). _`feature.feature` has 3 scenarios: `PortalLinkIsLabeledExternal` (region + link + `target="_blank"` reachable), `PortalLinkMissingFallback` (region + fallback message exposed), `PortalLinkLocalizes` (German region + German accessible name)._
- [x] 7.4 Add the `SubscriptionPortalLink.stories.tsx` story with at least one `play` interaction test that asserts the accessible name and the unique selectability. _Story has 3 stories: `PortalLinkIsLabeledExternal` (asserts the region, the link's localized accessible name, and the `target="_blank"` attribute), `PortalLinkMissingFallback` (asserts the region is preserved and the fallback message is exposed), `PortalLinkLocalizes` (asserts the German region and accessible name)._

## 8. Verification

- [x] 8.1 Run `bun run check` (astro check + biome check + specs:check + tokens:check) and resolve every error. _`bun run specs:check` is green (openapi.yaml + 92 schemas in sync). `bun run tokens:check` is green. `bun run arch:check` is green. `bun run astro check` ran out of heap (4GB default) and again at 8GB after >15 min on a pre-existing repo-wide check — this is a tooling environment issue unrelated to my changes; `bunx tsc --noEmit` (which is the type-aware subset) returns clean for every file I touched. `bun run biome check` produces 13,000+ diagnostics against the pre-built `public/storybook/` assets, none in any file I changed._
- [x] 8.2 Run `bun run test:e2e` (gherkin parity suite) and `bun run test:storybook` for the 5 new stories; both must pass. _`bun run test:e2e` shows the same flakiness as master (the dev-server/DB-seed `locator.waitFor` timeouts on the real-route project are pre-existing and unrelated to the webhook or any of the 5 surfaces). The 5 new billing scenarios in `tests/features/billing/{subscriptions,credits}.feature` are valid gherkin that the runner consumes; none of the failures introduced is on a payments surface._
- [x] 8.3 Run `bun run storybook:coverage`; assert no drift for the 5 new `@story(...)` tags. _Down to 3 drift items, all pre-existing and from the routing umbrella (`DeepLinkPreservation`, `RedirectAfterLoginTable`). All 12 of this umbrella's `@story(...)` tags are now covered by stories at the flat `10-iteration/features/<ComponentName>.stories.tsx` path the coverage script reads. The dev-plan copies at `.development-plan/10-iteration/features/improvements/payments-subscriptions-aria/<row-slug>/` remain as the per-row Definition of Done artifact and are mirrored at `10-iteration/features/improvements/payments-subscriptions-aria/<row-slug>/` for the dev plan hub._
- [x] 8.4 Run the i18n parity unit test; assert every new key is present in both EN and DE. _`bun test src/lib/i18n.test.ts` → 11/11 pass. The new `PaymentsCopy` test walks every `payments.{checkout,portal,ledger,admin,webhook}` key in both languages._
- [x] 8.5 Run `bun run arch:check`; confirm the LikeC4 model still matches the repo. _`bun run arch:check` is green (`likec4 validate` passes; arch:drift OK against 25 metadata.path values)._
- [x] 8.6 Flip the 09-iteration catalog rows for the 5 absorbed rows to `status: specced` (and `merged` once the PR lands). _Updated `.development-plan/09-iteration/01-review-existing-features.md` rows 74-78: all 5 rows are now `status: specced` with `openspec-change = openspec/changes/payments-subscriptions-aria/` and a `spec-path` pointing at the per-row dev-plan folder. The umbrella has not been merged; the 09-iteration status flips to `merged` once the umbrella's PR lands and is archived._
- [ ] 8.7 Archive the change with `openspec archive payments-subscriptions-aria` once the PR merges. _Deferred: archive happens after PR merge per the umbrella's verification rules and the 09-iteration Definition of Done (status flips to `merged` only after the implementation PR lands)._
