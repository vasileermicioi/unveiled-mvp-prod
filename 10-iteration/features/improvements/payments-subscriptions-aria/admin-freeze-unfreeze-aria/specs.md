# Specs — Row 4 — Admin freeze / unfreeze form

This per-row `specs.md` points at the umbrella's OpenSpec capability
deltas for the `admin-freeze-unfreeze-aria` row.

## Capability deltas

- `openspec/changes/payments-subscriptions-aria/specs/payments-subscriptions/spec.md`
  — `## MODIFIED Requirements` block on `Admin Billing Overrides`.
  Relevant scenarios:
  - `Admin freeze form is selector-disciplinable and accessible`
  - `Admin freeze form reports server errors accessibly`
