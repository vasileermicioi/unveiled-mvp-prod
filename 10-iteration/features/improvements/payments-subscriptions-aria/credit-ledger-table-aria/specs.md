# Specs — Row 3 — Credit ledger view table semantics

This per-row `specs.md` points at the umbrella's OpenSpec capability
deltas for the `credit-ledger-table-aria` row.

## Capability deltas

- `openspec/changes/payments-subscriptions-aria/specs/payments-subscriptions/spec.md`
  — `## MODIFIED Requirements` block on `Subscription Data Model`.
  Relevant scenario:
  - `Credit ledger view is a single accessible region`
