# Specs — Row 2 — Stripe webhook handler validation

This per-row `specs.md` points at the umbrella's OpenSpec capability
deltas for the `stripe-webhook-typespec` row.

## Capability deltas

- `openspec/changes/payments-subscriptions-aria/specs/payments-subscriptions/spec.md`
  — `## MODIFIED Requirements` block on `Provider Event Processing`.
  Relevant scenarios:
  - `Webhook handler is contract-driven`
  - `Webhook handler validation surfaces schema failures`
