# Specs — Row 5 — Subscription portal link

This per-row `specs.md` points at the umbrella's OpenSpec capability
deltas for the `subscription-portal-link-aria` row.

## Capability deltas

- `openspec/changes/payments-subscriptions-aria/specs/payments-subscriptions/spec.md`
  — `## MODIFIED Requirements` block on `Stripe Subscription Lifecycle`.
  Relevant scenario:
  - `Subscription portal link is selector-disciplinable and accessible`
